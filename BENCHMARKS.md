# Benchmarks

Performance targets from the PRD:

- 60-80% token reduction on typical sessions
- under 100ms compression latency for sessions up to 50k tokens

Benchmark coverage should start with the compression engine and store reads.

